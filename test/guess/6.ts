import test from "ava";
import {/* Ask, run */} from "../../src/guess/6";
import {/* perform */} from "../../src/util";

test.todo('guess/6', /* t => {
  t.plan();

  const handler = (effect: Ask) => {

  };

  t.deepEqual(await perform(run(), handler), );
} */)
